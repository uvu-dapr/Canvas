# DAPR 2020 Image Brief - v28 Corrections
Generated 2026-09-22. Run each prompt separately in ChatGPT. Save each result
with the exact filename shown, into the exact folder shown. Do not rename.

Written against `DAPR-2020-Course-Turnkey-v28.imscc`. The 39 course figures the
cartridge references were opened and looked at, composited on white first per 20.4b,
plus a sample of the 59 macOS Foundations screenshots. Nothing below was judged from
a filename or from alt text.

House style for this brief, per 20.5 and 20.5a:

| Rule | Value |
|---|---|
| Style | Photorealistic, unless the field table says dimensional diagram |
| Text inside the image | **None.** The page carries the heading, the caption and the prose |
| Palette for diagrams | `#212121` body dark, `#1B5E20` green, `#0D47A1` blue, `#B71C1C` red, `#993300` orange |
| Photorealistic format | JPEG, quality 88, its own background |
| Diagram format | PNG-24, transparent background |
| Never generated | Real hardware panels, real software windows, named products, silkscreen, model numbers (20.1 rule 3) |

---

## Image 01 - Solo against context

| Field | Value |
|---|---|
| Filename | `focus-and-balance-solo-versus-context-02.png` |
| Format | PNG-24, transparent background |
| Dimensions | 1600 x 900 px |
| Destination folder | `/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-2020--Core_Mixing/Images/Focus_and_Balance/` |
| Raw URL after push | `https://raw.githubusercontent.com/uvu-dapr/Canvas/main/Classes/DAPR-2020--Core_Mixing/_unplaced/Focus_and_Balance/Single_Clip_beside_Stacked_Track_Rows.png` |
| Used on | Canvas page `focus-and-balance-read-01` |
| Alt text | `Left, one track alone at full size. Right, the same track set among five others at the same scale` |
| Caption | `Figure 1. Left, soloed. Right, the same track in context. Nothing about the track changed. What changed is how much of it you can hear.` |
| Why | The shipping `-01` is illegible. The right panel stacks six waveforms and the blue solo trace is drawn larger than the rest, crossing and obscuring the red, orange and black traces underneath it. There are no panel titles, so a student cannot tell which side is the solo. Two dashed vertical lines appear with nothing indicating what they mark |

**ChatGPT prompt (paste as-is):**

```text
Create a 1600 x 900 pixel PNG with a fully transparent background.

A richly rendered dimensional diagram, viewed at a slight three quarter angle from
above, with real depth and solid color fills. Two panels of equal width side by side,
separated by a single thin vertical gray divider.

LEFT PANEL. One horizontal audio track lane, drawn as a long rounded slab with visible
thickness, sitting alone at the vertical center of the panel with clear empty space
above and below it. Its waveform is filled in blue #0D47A1, drawn as a symmetric
envelope about the lane center line, and it fills the full height of the lane.

RIGHT PANEL. Six horizontal audio track lanes of IDENTICAL size stacked vertically,
evenly spaced, each the same slab thickness and the same height as the single lane in
the left panel divided by six, so all six together occupy the same total panel height.

CRITICAL for the right panel: all six lanes are exactly the same size and none of them
overlaps, crosses, or is drawn on top of another. Each waveform stays entirely inside
its own lane and never spills past the lane boundary into the lane above or below.
Measure the lanes. If any one lane is taller, wider, brighter, or drawn in front of the
others, redraw it.

The third lane from the top in the right panel is filled in blue #0D47A1, matching the
left panel. The other five lanes are filled in neutral mid gray, all five the same
shade.

Reading direction is left to right for time.

No text anywhere in the image. No numbers, no letters, no labels, no track names, no
dashed vertical lines, no arrows, no callouts, no ruler markings. No borders, frames,
title bars, or caption text. No watermark, no signature.
```

---

## Image 02 - Quantize strength

| Field | Value |
|---|---|
| Filename | `timing-quantize-strength-comparison-02.png` |
| Format | PNG-24, transparent background |
| Dimensions | 1600 x 900 px |
| Destination folder | `/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-2020--Core_Mixing/Images/Timing/` |
| Raw URL after push | `https://raw.githubusercontent.com/uvu-dapr/Canvas/main/Classes/DAPR-2020--Core_Mixing/_unplaced/Timing/Marker_Rows_at_Varying_Offsets.png` |
| Used on | Canvas page `timing-read-01` |
| Alt text | `Three rows of drum hits against a grid, loose at the top, halfway in the middle, locked at the bottom` |
| Caption | `Figure 1. Top row, the raw performance. Middle row, 50 percent strength. Bottom row, 100 percent. Strength is how far each hit gets dragged toward the line, not whether it moves.` |
| Why | The shipping `-01` has no labels of any kind, so nothing says which row is which. Worse, the three rows are not a progression: the middle green row sits closest to the grid and both the black top row and the red bottom row sit off it, so even with a caption the picture contradicts any reading order |

**ChatGPT prompt (paste as-is):**

```text
Create a 1600 x 900 pixel PNG with a fully transparent background.

A richly rendered dimensional diagram, viewed at a slight three quarter angle from
above, with real depth and solid color fills.

Eight thin vertical gray planes stand evenly spaced across the full width of the frame,
running top to bottom, like a row of fence posts. These are the grid lines. They are
perfectly evenly spaced: measure the gaps and make them identical.

Three horizontal rows of drum hits, evenly spaced vertically. Each hit is a short
compact burst of waveform, drawn as a solid shape with visible thickness, roughly the
same size as every other hit. There are exactly eight hits in each row, one associated
with each grid plane.

TOP ROW, filled in red #B71C1C. Every hit is clearly OFF its grid plane, each one
offset by a different amount and in a different direction, some early and some late,
with the largest offsets about one fifth of the gap between grid planes. No hit in this
row touches its plane.

MIDDLE ROW, filled in orange #993300. Every hit sits about HALFWAY between where the
top row hit sits and its grid plane. Each offset is visibly smaller than the
corresponding offset in the top row but still visibly present. No hit in this row
touches its plane either.

BOTTOM ROW, filled in dark green #1B5E20. Every single hit is centered EXACTLY on its
grid plane, with zero offset. All eight are perfectly aligned.

CRITICAL: the three rows must read as a progression from top to bottom, loose to
locked. Measure the average offset in each row. Top row largest, middle row about half
of that, bottom row exactly zero. If the middle row is tighter than the bottom row, or
the bottom row is not perfectly aligned, redraw it.

Reading direction is left to right for time and top to bottom for increasing strength.

No text anywhere in the image. No numbers, no letters, no labels, no percentages, no
bar numbers, no ruler markings. No borders, frames, title bars, or caption text. No
watermark, no signature.
```

---

## Image 03 - Scale and target notes

| Field | Value |
|---|---|
| Filename | `timing-tuning-scale-and-target-notes-02.png` |
| Format | PNG-24, transparent background |
| Dimensions | 1600 x 900 px |
| Destination folder | `/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-2020--Core_Mixing/Images/Timing/` |
| Raw URL after push | `https://raw.githubusercontent.com/uvu-dapr/Canvas/main/Classes/DAPR-2020--Core_Mixing/_unplaced/Tuning/Scale_and_Target_Notes_Boxed.png` |
| Used on | Canvas page `tuning-read-01` |
| Alt text | `A piano roll with the in scale rows highlighted and a sung pitch line drifting between them` |
| Caption | `Figure 1. The highlighted rows are the notes in the key. The wavering line is what was sung. Correction pulls it to the nearest highlighted row, which is why the scale you pick decides the result.` |
| Why | Two defects on the shipping `-01`. First, it has no notes and no pitch line at all: it is an empty grid with colored horizontal rules, so a figure titled scale and target notes shows neither a scale nor a note. Second, the piano keyboard down the left edge has an irregular black key pattern. A real keyboard alternates groups of two and three black keys and this one does not, so the one concrete thing in the picture is wrong |
| Filename note | Renamed from `tuning-scale-and-target-notes-01.png` under 20.4a: the old name does not start with its folder slug. Same for the two files below |

**ChatGPT prompt (paste as-is):**

```text
Create a 1600 x 900 pixel PNG with a fully transparent background.

A richly rendered dimensional diagram, viewed straight on with soft depth and solid
color fills, like a piano roll editor rendered as a shallow relief.

Down the LEFT EDGE, a vertical piano keyboard seen end on, running the full height of
the frame. White keys are long pale slabs. Black keys are shorter dark slabs sitting
between them.

CRITICAL for the keyboard: the black keys follow the real piano pattern exactly, in
repeating groups of TWO black keys then THREE black keys, separated by the gaps where
two white keys sit side by side with no black key between them. Count them. Two, then
three, then two, then three, all the way up. If the groups are irregular, redraw the
keyboard.

To the right of the keyboard, a horizontal lane extends from each white and black key
across the full width of the frame, forming a grid. Thin vertical gray planes cross the
grid at even intervals, marking time.

SEVEN of those lanes are highlighted: filled in a soft translucent blue #0D47A1 tint
so they read as raised, active rows. The other lanes stay plain and pale. The seven
highlighted lanes are spread across the height of the frame in an uneven pattern, not
every other lane and not evenly spaced, because a musical scale is not evenly spaced.

Over the top of the grid, one continuous thick tube in dark green #1B5E20 travels left
to right, wavering smoothly up and down. It drifts across lane boundaries: sometimes it
runs along the middle of a highlighted blue lane, and sometimes it wanders off into a
plain lane between two highlighted ones. It must visibly cross in and out of the
highlighted rows rather than staying on one.

Reading direction is left to right for time and bottom to top for pitch.

No text anywhere in the image. No numbers, no letters, no note names, no key letters,
no labels, no bar numbers. No borders, frames, title bars, or caption text. No
watermark, no signature.
```

---

## Image 04 - Correction amount

| Field | Value |
|---|---|
| Filename | `timing-tuning-correction-amount-comparison-02.png` |
| Format | PNG-24, transparent background |
| Dimensions | 1600 x 900 px |
| Destination folder | `/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-2020--Core_Mixing/Images/Timing/` |
| Raw URL after push | `https://raw.githubusercontent.com/uvu-dapr/Canvas/main/Classes/DAPR-2020--Core_Mixing/_unplaced/Tuning/Correction_Amount_Comparison_Boxed.png` |
| Used on | Canvas page `tuning-read-02` |
| Alt text | `Three pitch traces against the same target line, wide vibrato at the top, reduced in the middle, dead flat at the bottom` |
| Caption | `Figure 2. Top, no correction. Middle, partial. Bottom, full. Full correction removes the vibrato along with the error, which is why it sounds synthetic.` |
| Why | The shipping `-01` has no labels, and the third panel drops the dashed target line entirely, so instead of reading as a trace snapped onto the target it reads as an unrelated plot. Without the target line in all three the comparison does not land |
| Filename note | Renamed from `tuning-correction-amount-comparison-01.png` under 20.4a |

**ChatGPT prompt (paste as-is):**

```text
Create a 1600 x 900 pixel PNG with a fully transparent background.

A richly rendered dimensional diagram, viewed at a slight three quarter angle from
above, with real depth and solid color fills. Three stacked panels of identical height,
evenly spaced, each one a shallow recessed tray so the three read as separate plots.

In EVERY one of the three panels, a thin horizontal plane in dark gray runs the full
width at the exact vertical center. This is the target pitch, and it must be present and
identical in all three panels.

TOP PANEL. One continuous thick tube in red #B71C1C travels left to right, wavering
smoothly above and below the gray target plane with large excursions, reaching about
forty percent of the panel height above and below the center at its widest.

MIDDLE PANEL. One continuous thick tube in orange #993300 travels left to right with
the SAME wave shape as the top panel, but every excursion is reduced to about one third
of its size. It still clearly rises and falls either side of the gray plane.

BOTTOM PANEL. One continuous thick tube in dark green #1B5E20 travels left to right
lying exactly along the gray target plane for its entire length, perfectly flat, with
no waver at all. It sits directly on top of the gray plane, not above it and not below.

CRITICAL: all three panels are the same height and all three contain the horizontal
gray target plane at the same vertical position. The three traces read as a progression,
large waver to small waver to none.

Reading direction is left to right for time and top to bottom for increasing
correction.

No text anywhere in the image. No numbers, no letters, no labels, no percentages, no
axis marks, no arrows. No borders, frames, title bars, or caption text. No watermark,
no signature.
```

---

## Image 05 - Crossfade placement

| Field | Value |
|---|---|
| Filename | `comping-and-arrangement-crossfade-placement-02.png` |
| Format | PNG-24, transparent background |
| Dimensions | 1600 x 900 px |
| Destination folder | `/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-2020--Core_Mixing/Images/Comping_and_Arrangement/` |
| Raw URL after push | `https://raw.githubusercontent.com/uvu-dapr/Canvas/main/Classes/DAPR-2020--Core_Mixing/_unplaced/Comping_and_Arrangement/Gray_Clips_with_Star_and_Crossfade.png` |
| Used on | Canvas page `comping-and-arrangement-read-01` |
| Alt text | `Left, an edit sitting on the transient. Right, the same edit moved back into the quiet part before it` |
| Caption | `Figure 1. Left, the edit lands on the attack and clicks. Right, the same edit moved back into the quiet part, where the crossfade has somewhere to hide.` |
| Why | Two smaller problems on the shipping `-01`. The two panel titles end in a colon with nothing after it, so both read as truncated. And the red dashed line means the edit point in the left panel but sits to the left of the crossfade in the right panel, so the same symbol carries two different meanings in one figure |

**ChatGPT prompt (paste as-is):**

```text
Create a 1600 x 900 pixel PNG with a fully transparent background.

A richly rendered dimensional diagram, viewed at a slight three quarter angle from
above, with real depth and solid color fills. Two panels of equal width side by side,
separated by a single thin vertical gray divider.

In BOTH panels, one horizontal audio track lane drawn as a long rounded slab with
visible thickness, running the full panel width. Inside each lane, a waveform drawn as
a symmetric envelope about the lane center line. The waveform has one obvious sharp
transient: a long quiet low amplitude stretch on the left, then a sudden tall spike,
then a decaying tail on the right. The two panels contain the identical waveform.

LEFT PANEL. A single thick vertical red #B71C1C plane cuts down through the lane
EXACTLY at the sharp transient spike, splitting the tall peak in two. A small red
#B71C1C starburst sits above the lane directly over that plane.

RIGHT PANEL. A single thick vertical dark green #1B5E20 plane cuts down through the
lane in the middle of the long quiet low amplitude stretch, well to the LEFT of the
transient spike, with clear empty quiet waveform on both sides of it. Centered on that
green plane, two smooth curved ramps cross each other in an X shape inside the lane,
one rising left to right and one falling left to right, both drawn as thin solid tubes.
The transient spike further right is untouched and complete. There is no starburst in
this panel.

CRITICAL: each panel contains exactly ONE vertical plane. Do not draw a second vertical
line anywhere in either panel.

Reading direction is left to right for time.

No text anywhere in the image. No numbers, no letters, no labels, no panel titles, no
colons, no arrows, no callouts. No borders, frames, title bars, or caption text. No
watermark, no signature.
```

---

# Not a ChatGPT job

## Adam captures or supplies these

| Item | What is needed | Why |
|---|---|---|
| `focus-and-balance-channel-strip-anatomy-01.png` | A real screenshot crop of one Pro Tools mix window channel strip | Two problems. It is a generated abstraction of a real software interface, which 20.1 rule 3 puts out of bounds. And it occupies about ten percent of its own 1600 px frame, so on a full width Canvas page it renders as a thin sliver between two enormous white margins. The XLR in it is drawn with three pins, which is correct, so the drawing is not wrong, just out of scope and badly composed |

## These should stop being pictures

| Item | Replace with |
|---|---|
| `course-orientation-and-feedback-grade-breakdown-01.png` | A table of the real category point values. **This figure asserts something false.** It is a stacked bar of four nearly equal segments, which tells a student the four categories are worth about 25 percent each. In v28 the final exam is 100 points out of 1,875 published, which is about 5 percent. The picture contradicts the gradebook |
| `professional-practice-network-drive-path-01.png` | The actual path in a code block on the page. The figure is a monitor icon, a generic share glyph and three colored folders. It names no server, no share and no folder, so it carries no information a student can act on |
| `course-orientation-and-feedback-learning-outcomes-01.png` | Nothing. Leave it out, which is where it is now. The six hexagons carry invented nicknames rather than the six course learning outcomes, so it does not teach the outcomes it is named for. It was always optional |

## Program level cleanups, build jobs rather than image jobs

### Seven live SVG files in the cartridge, against 20.1 rule 2

20.1 rule 2 says no SVG ever, for new artwork, with the single exemption of the
approved icon set under `Classes/All/DAPR_Canvas_Icon_Reference/`. These are not in
that folder. I checked the bytes: they are genuine hand authored `<svg viewBox=` markup
with `<text>` elements inside, not PNG binaries with an SVG extension, so 19.3 does not
cover them either.

Referenced live by v28:

```
macOS_Foundations/macOS_Foundations__App_Leftover_Files.svg
macOS_Foundations/macOS_Foundations__Cpu_Architecture_Timeline.svg
macOS_Foundations/macOS_Foundations__Dmg_Pkg_Exe_Flow.svg
macOS_Foundations/macOS_Foundations__File_System_Timeline.svg
macOS_Foundations/macOS_Foundations__Hardware_Software_Firmware_Stack.svg
macOS_Foundations/macOS_Foundations__Launch_Agents_vs_Daemons.svg
macOS_Foundations/macOS_Foundations__Privacy_Security_Pane.svg
```

Present but unreferenced:

```
macOS_Foundations/macOS_Foundations__Leftover_Folder_Diagram.svg
macOS_Foundations/macOS_Foundations__Root_Folder_Tree.svg
macOS_Foundations/macOS_Foundations__Dark_Toolbar_with_Labeled_Capture_Buttons.svg
```

Rasterizing them to PNG at 1600 px keeps the content exactly as it is and satisfies the
rule. Regenerating them through ChatGPT would risk the content, since each one is a
correct little diagram carrying real macOS folder paths. Rasterize, do not regenerate.

One of them also has a content nit: `hardware_software_firmware_stack.svg` prints
`CPU, RAM, storage, ports - physical`, using a spaced hyphen where a dash was intended.
Your standing rule has no dashes of any kind, so that becomes a comma or a colon.

### Sixty four filenames in macOS_Foundations, against 20.4 and 20.4a

Every file in that folder uses underscores, none starts with the folder slug, and none
carries a two digit revision suffix. 20.4 requires all lowercase, hyphens only, no
underscores, the topic slug as the prefix, and a `-NN` counter. 20.4a says a
non conforming name is renamed on sight, without asking, with every reference rewritten
in the same pass. Fifty nine of the sixty four are referenced live by v28.

Examples of what they become:

```
about_this_mac.png            ->  macos-foundations-about-this-mac-01.png
disk_utility_erase_dialog.png ->  macos-foundations-disk-utility-erase-dialog-01.png
cmd_opt_h_before.jpg          ->  macos-foundations-cmd-opt-h-before-01.jpg
```

This is one scripted pass over the folder plus one pass over `wiki_content`, then a
rebuild. Say the word and I will run it.

### Five more misnamed files in other folders

```
Comping_and_Arrangement/Comping_and_Arrangement__Pro_Tools_Playlist_Selector_New_Playlist.png
Focus_and_Balance/Focus_and_Balance__Pro_Tools_Mix_Window.png
Pro_Tools_Fundamentals/Pro_Tools_Fundamentals__PT_Identify_Beat_Bar_Beat_Markers.jpg
Spectral_Effects_and_EQ/Spectral_Effects_and_EQ__Vocal_EQ_Frequency_Band_Chart.jpg
Timing/Timing__Pro_Tools_Tce_Plugin_Preferences.png
```

None starts with its folder slug. Two use capitals and underscores. One has an uppercase
extension. Three have no revision suffix.

There is also a near duplicate pair sitting in one folder, which 20.4a covers:

```
Calibration_and_Monitoring/Calibration_and_Monitoring__Gain_Stages_Chain.png
Calibration_and_Monitoring/Calibration_and_Monitoring__Gain_Staging_Chain.png
```

Only `gain-staging-chain` is referenced. The other is an orphan under a name one letter
different, which is how the wrong one gets linked later.

### Six screenshots too small for a content figure

Native sizes, scaled up to content width they go soft:

```
pro-tools-tce-plugin-preferences.png           353 x 106
pro-tools-playlist-selector-new-playlist.png   333 x 253
PT_identify_beat_bar_beat_markers.jpg          383 x  92
dynamic-effects-dyn3-compressor-controls-01    457 x 153
balancing-and-reverb-d-verb-algorithms-01      455 x 376
delay-and-stereo-enhancements-moogerfooger-01  502 x 462
```

Recapture at a higher display scale, or constrain them with an explicit width on the
page rather than letting `max-width:100%` stretch them.

## Checked and clean

| Item | Result |
|---|---|
| `macos-foundations` erase dialog | Read for personal data per 20.4c. Clean. Every volume name in the sidebar is generic. No phone number, no user name, no serial number, unlike the DAPR 2000 capture that prompted that rule |
| `go_to_folder_autocomplete.png` | Clean. Shows only system paths and plug-in names |
| `focus-and-balance-five-pass-mix-order-01.png` | Correct and legible. Balance, subtractive, dynamics, placement, automation |
| `dynamic-effects-attack-too-fast-transient-loss-01.png` | Correct. Fast attack flattens the initial peak, slow attack lets it through |
| `professional-practice-bounce-truncation-01.png` | Correct and readable without labels |
| `master-buss-processing-and-endgame-headroom-on-delivery-01.png` | Nothing false. Carries no decibel values, so the ceiling the course teaches has to come from the page prose |
| All 113 image references in v28 | Resolve against the live repo |
