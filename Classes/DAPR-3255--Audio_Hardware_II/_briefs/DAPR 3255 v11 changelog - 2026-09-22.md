# DAPR 3255 v11 changelog

2026-09-22

```
/mnt/user-data/outputs/DAPR-3255-Course-Turnkey-v11.imscc
```

1,793,824 bytes. Built, zipped, unzipped to a different folder, gated there. PASS,
hard fails empty.

You told me to read the instructions properly. I had not been. Two rules in
Standards §20 changed the answer once I applied them, and reading the cartridge
skill's step 6 sent me to ten images I had never opened.

## 1. §20.4b: composite on white before judging

A transparent PNG viewed on a dark background shows white and colored speckle at
every edge. §20.4b exists because a figure was rejected on exactly that basis and
turned out to be the cleanest in its set.

I did it anyway, twice, and put a retry prompt in the brief for one of them.

| Figure | What I reported | On white |
|---|---|---|
| `network-diagnostics-arp-resolution-sequence-02.png` | red and cyan speckle across the frame | clean. Four broadcast arrows, one reply, correct |
| `ip-addressing-nat-translation-flow-02.png` | speckle at the edges | clean |

Every image in the course is now composited on white in
`Images/_briefs/_review/white/` and that is what I judged from.

## 2. The final exam: ten images, eleven graded questions, never opened

The skill says to open every image that appears in a quiz. Ten schematic symbols
carry eleven questions on the final at one point each. **Not one had ever been
opened, in any version.** Three were wrong.

| Image | Defect | Fixed |
|---|---|---|
| `final-exam-symbol-07.png` | The word **SPST** is printed under the symbol. SPST is the keyed answer. The question was a free point | Cropped to `-07-02`, answer removed |
| `final-exam-symbol-04.png` | Three inductor variants, the third the International filled rectangle. **Resistor** is a distractor on this question, and the International resistor on question 06 is the same rectangle | Cropped to `-04-02`, US coil forms only |
| `final-exam-symbol-10.png` | Stem reads "the symbols shown below are common ground symbols". Half the image is VCC, 5V and V+ **power** flags | Cropped to `-10-02`, grounds only |

All three are sourced schematic art, so §20.1 rule 3 forbids regenerating them.
Cropping is the fix. New files carry `-02` per §20.4, the originals stay on disk,
the quiz is repointed, and each new file is declared as its own webcontent
resource in the manifest. Verified by resolving every `$IMS-CC-FILEBASE$`
reference against both the file on disk and the manifest, not by counting them.

The other seven are correct. Symbol 05 answers both questions that use it.

## 3. A four-pin XLR on a student page

`network-foundations-analog-snake-vs-network-01.png` fans an analog multicore out
to fourteen connectors. **Every connector face is drawn with four holes.** An XLR
has three pins.

It has been on `Networking: Introduction to Audio Networking` since v2, in a
course that teaches pin 2 hot and pin 3 cold on the Talkback Box build in the
same semester. §20.1 rule 3, third scalp for the program after the XLR with two
pins and the DIN numbered wrong in DAPR 2255.

Worse: it was on the "cannot be generated" list in my own brief v7, where I wrote
"Leave, or photograph the real thing." I left it and never looked at it.

**Removed from the page.** It cannot be re-prompted. It is now on the capture
sheet as a photograph to take, which takes that list from eight shots to nine.

## 4. §20.5: the image is not the delivery mechanism

"Every fact the image states is also stated in the page prose or the caption."
And §20.5a: when a subject does not fit a picture, the honest answer is an HTML
table on the page rather than a picture at all.

I had spent two versions arguing with myself about labelled figures against
unlabelled ones. The rule was never about the image.

| Page | Fixed |
|---|---|
| `Networking: Subnet Masks and CIDR` | `ip-addressing-cidr-block-sizes-01.png` **is a table**, seven prefixes against seven host counts, rendered as pixels. Replaced with a real HTML table with header cells and a caption |
| `Networking: IP Address Ranges to Memorize` | Caption claimed the bars were "drawn to relative scale". A /8 is 256 times a /16 and the bar is about four times longer. Caption now states the true ratio in words |
| `Networking: IP Addressing Fundamentals` | The figure works 192.168.10.57 against 255.255.255.0 and the page never named either. Caption now does |
| `Networking: AVB and Milan` | The figure prints a 75 percent reserved, 25 percent best effort split that appeared nowhere in the page text. Caption now carries it |

Also removed `network-diagnostics-mirror-against-tap-01.png` from the Wireshark
capture page. I put it there in v9 without noticing `capture-point-placement-01`
was already on that page showing a mirror and a tap, labelled and better.

## 5. A wireless figure was already done, and Dropbox had hidden it

`wireless-microphone-against-iem-direction-01.png` exists, 1600 x 900,
transparent PNG, correct: three transmitters converging on one receiver, one
transmitter fanning out to three receivers.

I missed it in three passes because Dropbox had filed it under

```
wireless (Adam Olson's conflicted copy 2026-09-22)
```

not `wireless`. §20.4a renames a non conforming name on sight, so the file is now
in a proper `wireless/` folder and **placed** on `Wireless: System Architecture`,
which had no figure at all.

**One thing for you:** I cannot delete, so the conflicted folder is still there
and the repo would carry the picture twice. Delete it by hand:

```
/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-3255--Audio_Hardware_II/Images/wireless (Adam Olson's conflicted copy 2026-09-22)
```

## 6. The Image Reference, which I should have shipped three versions ago

§8.1 and §20.8: the Image Reference HTML is a mandatory build artifact and **no
.imscc ships alone.** I shipped v9 and v10 without it.

Regenerated and written to the repo root:

```
/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-3255--Audio_Hardware_II/DAPR-3255--Audio_Hardware_II--Image-Reference.html
```

107 images, 8 sections, one per direct subfolder, alphabetical, raw
githubusercontent URLs. Every src resolves to a file that exists and every file
on disk appears in it, both checked by resolving rather than counting. The
conflicted copy folder is excluded.

## 7. The brief is five prompts

| | Prompts |
|---|---|
| v7 | 51 |
| v8, after finding you had already run 47 | 11 |
| v9, after checking the `-01` files | 7 |
| v10, after the full review on white | 6 |
| **v10 as shipped, after finding the hidden wireless figure** | **5** |

```
/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-3255--Audio_Hardware_II/Images/_briefs/ChatGPT Image Creation - DAPR 3255 v10.md
```

Three are the wireless figures still missing. Two are real defects:

| Prompt | Why |
|---|---|
| 04, op-amp inverting against non-inverting | The `-01` on the page has **no input at all** on the non-inverting half. The `-02` put the feedback on the plus input. Two wrong versions, so the prompt spells the wiring out node by node |
| 05, three filter alignments | The `-01` has the stopband slopes in reverse order, which is why it was never placed |

**Prompt 04 is the only one of the five a student can see today.**

## 8. What I checked, and at what fidelity

- **All 10 final exam images**: full resolution, on white, against the question
  stem and the keyed answer for each
- **All 32 legacy `-01` figures in use**: on white, four-up sheets, escalating to
  full resolution on anything asserting a value or showing a connector
- **All 47 figures generated on 2026-09-22**: reviewed in the previous pass, plus
  the two speckle condemnations rechecked on white
- **`network-foundations-t568a-t568b-pin-order-01.png`**: verified wire by wire
  earlier and unchanged
- **Talkback Box photographs**: your own, screened for personal data in v6

Findings beyond the ones above: none. The remaining figures are correct. The
DHCP handshake is in DORA order with the right directions, the diagnostic ladder
has the right tool on every rung, the OSI stack has all seven layers in order,
the CIDR host counts are right, the IPv6 split is 48/16/64, the crossover crosses
exactly the four conductors it should, and ST 2110-20, -30 and -40 are video,
audio and data respectively.

## 9. Still on you

1. Delete the Dropbox conflicted copy folder, section 5.
2. `git add`, `commit`, `push` the image repo, including the three cropped exam
   symbols, the new `wireless/` folder and the Image Reference.
3. Run the five prompts, or just prompt 04 for the minimum that fixes what
   students see.
4. Photograph the multicore fan-out, section 3.
5. The OCR census takes longer than the shell's time limit to run over 107
   images. Run it when convenient and rule 20.5d turns itself on:

```
bash "/Users/adamwolson/Library/CloudStorage/Dropbox/Reference Files/Applications/AI Projects Standards/Work/Utah Valley University/Build Tools/ocr_census.sh" "/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-3255--Audio_Hardware_II/Images"
```

Still open from before: the Dante Level 3 catalog description, rubrics for two
Bonus-group assignments, the Small Bear kit SKU `KIT-AudHW2-PTB` price, and the
`Final/Final_Exam_-_Study_Guide/` folder of 32 scraped images.
