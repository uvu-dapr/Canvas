# DAPR 3255 v13 changelog

2026-09-23

```
/mnt/user-data/outputs/DAPR-3255-Course-Turnkey-v13.imscc
```

1,795,244 bytes. Built, zipped, unzipped to a different folder, gated there. PASS.

The layout changed under the course. v12 pointed every image at
`Images/<topic>/<topic>-<subject>-NN.ext`, and that shape no longer exists. This
release moves the cartridge onto the flat layout and fixes two things found on the
way.

## 1. Every image URL rewritten

74 URLs rewritten from this course's own `DAPR-3255--Audio_Hardware_II__Rename_Handoff.md`,
which carries all 122 old to new pairs. I matched on the old path and wrote the
new one exactly as the handoff gives it, no re-casing and no guessing.

Verified two ways rather than counted:

- All 122 new paths in the handoff exist on disk.
- All 82 course URLs the cartridge now carries resolve to a real file, checked
  against the actual directory listing rather than `os.path.exists`, because macOS
  folds case and both hosts do not. Zero misses, zero case mismatches.

Ten more URLs point at the shared icon set under `All/DAPR_Canvas_Icon_Reference/`,
which this rename did not touch. They still resolve.

**Timing matters here.** GitHub is not pushed, so it still serves the old paths.
v12's URLs work today and die the moment you push. v13's URLs are the reverse.
Ship v13 with the push, not before it.

## 2. I had duplicated a set you already had

Yesterday I rebuilt the schematic symbols page and cut ten symbol images out of
the final exam art to do it. You have had that same artwork in
`Electronics/Schematic_Symbols/` since April, and the final exam study guide has
been using it the whole time. I compared them and they are the same drawings.

Your set is also larger. It carries four the exam art does not:
`ac_dc_source`, `diode_schottky_zener`, `variable_resistor`, `voltage_ground`.

The schematic symbols page now points at your folder. My ten copies are in:

```
/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-3255--Audio_Hardware_II/_to_delete
```

I should have looked for existing art before cutting my own. The course now has
one symbol set instead of two, used by both the study guide and the symbols page.

## 3. Four filenames the rename got wrong against its own rule

The standard says acronyms are fully uppercase and that case is load bearing
because both hosts are case sensitive. Four files came out of the rename with a
Title Cased acronym:

| Was | Now |
|---|---|
| `Electronics/Xlr_Female_Pin_Numbering.jpg` | `Electronics/XLR_Female_Pin_Numbering.jpg` |
| `Electronics/Peak_P2p_RMS_Voltage.jpg` | `Electronics/Peak_P2P_RMS_Voltage.jpg` |
| `Electronics/Talkback_Box_Din_Solder_Joints.jpg` | `Electronics/Talkback_Box_DIN_Solder_Joints.jpg` |
| `Wireless/Microphone_Against_Iem_Direction.png` | `Wireless/Microphone_Against_IEM_Direction.png` |

Renamed on disk and rewritten in the cartridge in the same pass, which is the
discipline your rule 4 asks for. This was free to do now because nothing is
pushed. After a push it breaks four live URLs.

**Eight more sit in two older folders and I did not touch them**, because both
predate this rename and your rule 1 says folder names come from the module list,
which is still open:

- `Electronics/Schematic_Symbols/` is entirely lowercase: `ac_dc_source.png`,
  `switch_spst.png`, `transistor_fet.png` and ten others.
- `Final/Final_Exam_-_Study_Guide/` is the same: `ac_dc.png`, `switches_spst.png`,
  `diode_led_photodiode.png`.

Both are referenced by live pages, so renaming them means rewriting the cartridge
too. Say the word and it is one pass.

## 4. One SVG on a student page

```
Electronics/MIDI_to_XLR/MIDI_to_XLR_Pinout.svg
```

Referenced from the MIDI to XLR Cables bonus assignment. §20.1 rule 2 bans SVG for
anything but the approved icon set, and this is a **pinout**, which is also §20.1
rule 3 territory. It predates this session and I have left it alone rather than
touch a bonus assignment without asking, but it is the only SVG in the course
outside the icon set.

## 5. Image Reference regenerated for the flat layout

```
/Users/adamwolson/Library/CloudStorage/Dropbox/apps/GitHub/Canvas/Classes/DAPR-3255--Audio_Hardware_II/DAPR-3255--Audio_Hardware_II--Image-Reference.html
```

161 images, 12 sections, one per module folder including the three nested ones.
Every src resolved against a real file. The old one described the `Images/` tree
and was wrong the moment the rename ran.

## 6. What did not change

The image review from v11 and v12 stands. The three cropped exam symbols, the
four-pin XLR removal, the rebuilt schematic symbols page, the CIDR table, the four
caption fixes and all four wireless figures are all still in place and all now
point at the new paths.

75 files in the course folders are referenced by nothing. That is expected and not
a defect: §20.4 keeps the old file on disk when a page's `src` moves, so the whole
reverted `-02` restyle generation is sitting there by design.

## 7. On you

1. **Push GitHub and ship v13 together.** v12 and a pushed repo are incompatible in
   both directions.
2. Empty `_to_delete/`, which holds my ten duplicate symbol copies.
3. Decide on the two lowercase folders in section 3 and the SVG in section 4.
4. Run the two prompts in `ChatGPT Image Creation - DAPR 3255 v11.md`, or just
   prompt 01, the op-amp.
5. Photograph the multicore fan-out; source a BJT symbol.

The OCR label census is stale now that every filename changed, so preflight rule
20.5d reports that it could not run. It keyed on the `-01` and `-02` revision
counters, which the new standard removes entirely, so that rule needs rethinking
rather than just a rerun. Flagging it rather than quietly leaving a dead gate.

Still open from before: the Dante Level 3 catalog description, rubrics for two
Bonus-group assignments, the Small Bear kit SKU `KIT-AudHW2-PTB` price.
